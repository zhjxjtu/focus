<table class="table table-bordered">
  <tr>
  	<td><strong>Name</strong></td>
  	<td><strong>Email</strong></td>
  	<td><strong>Phone Number</strong></td>
  	<td><strong>Description</strong></td>
  </tr>
  <% unless @invitees == nil %>
    <% @invitees.each do |invitee| %>
      <tr>
        <td><%= invitee.name %></td>
        <td><%= invitee.email %></td>
        <td><%= invitee.phone_number %></td>
        <td><%= invitee.description %></td>
      </tr>
    <% end %>
  <% end %>
  <% unless @invitees == nil %>
    <% @inviters.each do |inviter| %>
      <tr>
        <td><%= inviter.name %></td>
        <td><%= inviter.email %></td>
        <td><%= inviter.phone_number %></td>
        <td><%= inviter.description %></td>
      </tr>
  <% end %>
  <% end %>
</table>